package br.sapiens.bellus_app.utils

import br.sapiens.bellus_app.R

object Contantes {

    object Firebase {
        val CLIENT_ID = (R.string.default_web_client_id).toString()
    }

    object Firestore {
        const val USERS = "users"
    }
}