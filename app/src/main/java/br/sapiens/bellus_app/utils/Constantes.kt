package br.sapiens.bellus_app.utils

import br.sapiens.bellus_app.R

object Contantes {

    object Firebase {
        val CLIENT_ID = (R.string.firebase_web_key).toString()
    }

    object Firestore {
        const val USERS = "users"
    }
}